<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Consent</title>
</head>
<body>

<h1>dev consent</h1>

<div>
    <% if(error) { %>
    <div style="color: red;"><%= error %></div>
    <% } %>
    <div>
        Would you like to authorize <%= clientId %> to access the following information:
        <ul>
            <% scopes.forEach((scope) => { %>
            <li><%= scope %></li>
            <% }) %>
        </ul>
    </div>
</div>

<form method="post" action="/interaction/<%= uuid %>/complete">
    <input type="hidden" name="uuid" value="<%= uuid %>"/>
    <input type="hidden" name="uid" value="<%=uid%>"/>
    <input type="hidden" name="status" value="success"/>
    <input type="hidden" name="type" value="consent"/>
    <button type="submit">Continue</button>
</form>

</body>
</html>